---
phase: 02-extraction
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/services/classification_service.py, src/services/extraction_service.py, tests/test_extraction.py]
autonomous: true
requirements: [REQ-EXT-01, REQ-EXT-02, REQ-EXT-03, REQ-EXT-04]

must_haves:
  truths:
    - "Classification service identifies documents based on cues"
    - "Extraction service returns structured JSON Triplets using response_schema"
    - "Pydantic validation ensures schema compliance for all extractions"
  artifacts:
    - path: "src/services/classification_service.py"
      provides: "Agentic classification logic"
    - path: "src/services/extraction_service.py"
      provides: "Gemini-powered structured extraction"
  key_links:
    - from: "src/services/extraction_service.py"
      to: "google-genai"
      via: "response_schema API"
---

<objective>
Implement the two-step agentic extraction logic: Classification of documents using cues, followed by detailed extraction using structured schemas and Gemini 1.5 Pro.

Purpose: Increase extraction reliability by correctly identifying document types before applying specialized schemas.
Output: Verified classification and extraction services.
</objective>

<execution_context>
@C:/Users/sarka/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/sarka/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-extraction/02-01-PLAN.md
@src/models/triplets.py
@configs/bank_paydown.json

<interfaces>
From src/services/gemini_client.py:
```python
def get_gemini_client(): ...
```

From src/models/triplets.py:
```python
class Triplet(BaseModel):
    value: Any
    confidence: float
    bbox: List[int]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Classification Service</name>
  <files>src/services/classification_service.py</files>
  <action>
    Implement `ClassificationService` class:
    - `classify(content: bytes, mime_type: str, cues: List[str]) -> str`:
      - Uses Gemini 1.5 Pro to identify document type based on provided `classification_cues`.
      - Construct a prompt that includes the cues and asks for a simple string response from a known list of types.
      - Handle image/PDF inputs.
    - Implement a method to load cues from `configs/`.
  </action>
  <verify>
    <automated>python -c "from src.services.classification_service import ClassificationService; print('Success')"</automated>
  </verify>
  <done>Classification logic implemented using Gemini 1.5 Pro.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Extraction Service with Structured Output</name>
  <files>src/services/extraction_service.py, tests/test_extraction.py</files>
  <action>
    Implement `ExtractionService` class:
    - `extract(content: bytes, mime_type: str, schema: Dict) -> TripletResult`:
      - Uses Gemini 1.5 Pro with `response_schema` (structured output) to extract fields as Triplets.
      - Map the `configs/bank_paydown.json` schema to a Gemini-compatible Pydantic schema or direct JSON schema.
      - Constructed prompt should emphasize spatial accuracy for bounding boxes.
      - Use `temperature=0` for maximum consistency.
    - Implement validation against the target Pydantic models defined in 02-01.
    - Create `tests/test_extraction.py` with mock/sample responses to verify the logic.
  </action>
  <verify>
    <automated>pytest tests/test_extraction.py</automated>
  </verify>
  <done>Extraction service implemented with structured output and Pydantic validation.</done>
</task>

</tasks>

<verification>
Verify classification correctly labels sample documents and extraction returns valid Triplet structures.
</verification>

<success_criteria>
- Classification service correctly identifies a document given cues.
- Extraction service returns structured JSON with value, confidence, and bounding boxes.
- Pydantic validation passes for all extraction outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/02-extraction/02-02-SUMMARY.md`
</output>
