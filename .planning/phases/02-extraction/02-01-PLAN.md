---
phase: 02-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/models/schema.py, src/models/triplets.py, src/services/coordinate_scaler.py, requirements.txt]
autonomous: true
requirements: [REQ-EXT-03, REQ-EXT-05, REQ-NFR-03]

must_haves:
  truths:
    - "SQLite schema supports Extractions table with link to Packages"
    - "ExtractedFile records store image width and height"
    - "Coordinate scaling utility maps 500 to 50% image dimensions"
  artifacts:
    - path: "src/models/triplets.py"
      provides: "Pydantic models for BoundingBox and Triplet"
    - path: "src/services/coordinate_scaler.py"
      provides: "CoordinateScaler class with pixel normalization"
    - path: "src/services/gemini_client.py"
      provides: "Client factory for google-genai"
  key_links:
    - from: "src/services/coordinate_scaler.py"
      to: "Pillow"
      via: "image dimension scaling"
---

<objective>
Establish the technical foundation for core extraction by updating the database schema, defining the Triplet Pydantic models, and implementing the Coordinate Scaler utility.

Purpose: Ensure consistent data structures for extraction results and enable mapping between Gemini's normalized coordinates and actual image pixels.
Output: Updated database schema, new Triplet models, and a verified Coordinate Scaler.
</objective>

<execution_context>
@C:/Users/sarka/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/sarka/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/models/schema.py
@src/services/ingestor.py

<interfaces>
From src/models/schema.py:
```python
class Package(Base):
    id = Column(String(36), primary_key=True)
    status = Column(String) # INGESTED, EXTRACTED, APPROVED

class ExtractedFile(Base):
    id = Column(Integer, primary_key=True)
    package_id = Column(String(36), ForeignKey("packages.id"))
    filename = Column(String)
    content = Column(LargeBinary)
    mime_type = Column(String)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Schema and Define Triplet Models</name>
  <files>src/models/schema.py, src/models/triplets.py</files>
  <action>
    1. Create `src/models/triplets.py` with Pydantic models for the "Triplet Object" standard:
       - `BoundingBox`: [ymin, xmin, ymax, xmax] as list of ints (0-1000 normalized).
       - `Triplet`: Generic model with `value` (Any), `confidence` (float), and `bbox` (BoundingBox).
       - Define specific Pydantic models for the expected extraction output (e.g., `ExtractionResult` containing fields as Triplets).
    2. Update `src/models/schema.py`:
       - Add `Extractions` table:
         - `id` (Integer, PK)
         - `package_id` (String, FK to packages.id)
         - `file_id` (Integer, FK to extracted_files.id, nullable=True for package-level extractions)
         - `document_type` (String)
         - `extraction_json` (JSON or Text for raw triplets)
         - `confidence_score` (Float, aggregate)
         - `created_at` (DateTime)
       - Add `width` and `height` (Integer) to `ExtractedFile` to store image/PDF dimensions.
  </action>
  <verify>
    <automated>python -c "from src.models.schema import Extractions; from src.models.triplets import Triplet; print('Success')"</automated>
  </verify>
  <done>Database schema updated and Pydantic models defined.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Coordinate Scaler Utility</name>
  <files>src/services/coordinate_scaler.py, tests/test_scaler.py</files>
  <action>
    Implement `CoordinateScaler` class in `src/services/coordinate_scaler.py`:
    - `normalize_to_pixel(bbox, img_width, img_height)`: Converts Gemini [0, 1000] list to [ymin, xmin, ymax, xmax] in pixels.
    - `pixel_to_normalize(bbox, img_width, img_height)`: Inverse conversion.
    - Add robust error handling for out-of-bounds coordinates.
    Create `tests/test_scaler.py` to verify scaling logic with known image dimensions (e.g., 2000x3000).
  </action>
  <verify>
    <automated>pytest tests/test_scaler.py</automated>
  </verify>
  <done>Coordinate scaling logic implemented and verified with tests.</done>
</task>

<task type="auto">
  <name>Task 3: Setup Gemini SDK and Environment</name>
  <files>requirements.txt, src/services/gemini_client.py</files>
  <action>
    1. Update `requirements.txt`: Add `google-genai` and `pdf2image`.
    2. Create `src/services/gemini_client.py`:
       - Implement a factory/singleton for the Gemini client using `google-genai`.
       - Load `GEMINI_API_KEY` from environment variables.
       - Implement a simple connectivity check method.
       - Ensure the client is initialized with the correct model (`gemini-1.5-pro`).
  </action>
  <verify>
    <automated>python -c "from src.services.gemini_client import get_gemini_client; print('Client initialized')"</automated>
  </verify>
  <done>Gemini SDK configured and client factory implemented.</done>
</task>

</tasks>

<verification>
Run all unit tests for models, scaler, and client initialization.
</verification>

<success_criteria>
- Pydantic Triplet models correctly validate sample data.
- Coordinate scaler maps 500 to 50% of image dimensions accurately.
- Database schema supports the new `Extractions` table.
- `google-genai` is successfully imported and initialized.
</success_criteria>

<output>
After completion, create `.planning/phases/02-extraction/02-01-SUMMARY.md`
</output>
