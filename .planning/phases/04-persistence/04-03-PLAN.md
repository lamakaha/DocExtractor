---
phase: 04-persistence
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: [src/ui/dashboard.py, src/ui/reviewer.py, src/services/export_service.py]
autonomous: false
requirements: [REQ-EXP-01, REQ-NFR-01]

must_haves:
  truths:
    - "User can download an Excel report from the Dashboard"
    - "User can download an Excel report for a single package from the Reviewer"
    - "System handles missing fields or empty transaction lists without crashing"
  artifacts:
    - path: "src/ui/dashboard.py"
      provides: "Bulk export button"
    - path: "src/ui/reviewer.py"
      provides: "Single package export button"
  key_links:
    - from: "src/ui/dashboard.py"
      to: "src/services/export_service.py"
      via: "Excel generation call"
---

<objective>
Integrate the Excel export functionality into the Streamlit UI and ensure the system is resilient to missing or malformed data during the export process.
</objective>

<execution_context>
@C:/Users/sarka/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/services/analytical_service.py
@src/services/export_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard Bulk Export Integration</name>
  <files>src/ui/dashboard.py</files>
  <action>
    - Add an "Export All Approved" button to the Dashboard sidebar or main view.
    - Fetch all packages with `APPROVED` status.
    - Use `AnalyticalService` to fetch summary and transactions for these packages.
    - Use `ExcelExporter` to generate the file.
    - Provide a `st.download_button` for the user.
  </action>
  <verify>
    <automated>python -c "import streamlit; print('Streamlit integration ready')"</automated>
  </verify>
  <done>Bulk export button added to the Dashboard.</done>
</task>

<task type="auto">
  <name>Task 2: Reviewer Single Export Integration</name>
  <files>src/ui/reviewer.py</files>
  <action>
    - Add an "Export This Package" button to the Reviewer interface (visible only if status is `APPROVED` or immediately after approval).
    - Generate and provide a `st.download_button` for the specific package.
  </action>
  <verify>
    <automated>python -c "import streamlit; print('Streamlit integration ready')"</automated>
  </verify>
  <done>Single package export button added to the Reviewer.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Resilience Handling</name>
  <files>src/services/export_service.py</files>
  <action>
    - Update `ExcelExporter` to handle missing fields by inserting "N/A" or reasonable defaults.
    - Ensure empty transaction lists result in an informative message or empty table instead of an error.
    - Add error logging for failed export attempts.
  </action>
  <verify>
    <automated>pytest tests/test_export.py</automated>
  </verify>
  <done>Resilience handling implemented and verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>End-to-End Excel Export from UI.</what-built>
  <how-to-verify>
    1. Run: streamlit run src/ui/app.py
    2. Go to Dashboard and click "Export All Approved".
    3. Ensure a valid Excel file is downloaded.
    4. Go to Reviewer for an APPROVED package and click "Export This Package".
    5. Ensure a valid Excel file is downloaded.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Verify bulk and single package exports from the UI.
</verification>

<success_criteria>
- Bulk export button functional on Dashboard.
- Single export button functional on Reviewer.
- Exported files are correctly formatted even with missing data.
- User experience is smooth and informative.
</success_criteria>

<output>
After completion, create .planning/phases/04-persistence/04-03-SUMMARY.md
</output>
