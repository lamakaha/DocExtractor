---
phase: 04-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/services/analytical_service.py, requirements.txt]
autonomous: true
requirements: [REQ-DAT-01, REQ-DAT-02]

must_haves:
  truths:
    - "DuckDB can successfully attach the SQLite packages.db"
    - "Analytical views correctly shred JSON triplets into flat columns"
  artifacts:
    - path: "src/services/analytical_service.py"
      provides: "DuckDB connection and analytical views"
  key_links:
    - from: "src/services/analytical_service.py"
      to: "DuckDB"
      via: "SQLAlchemy/SQLite attachment"
---

<objective>
Implement the DuckDB Analytical Layer to "shred" extraction JSON triplets into a columnar format for high-speed reporting and querying.
</objective>

<execution_context>
@C:/Users/sarka/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/models/schema.py
@src/models/triplets.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup DuckDB and SQLite Integration</name>
  <files>requirements.txt, src/services/analytical_service.py</files>
  <action>
    - Add `duckdb` to `requirements.txt`.
    - Implement `AnalyticalService` class in `src/services/analytical_service.py`.
    - Initialize a DuckDB connection.
    - Attach the SQLite `packages.db` using DuckDB's `sqlite` extension.
    - Ensure the connection is persistent or handled via a context manager.
  </action>
  <verify>
    <automated>python -c "import duckdb; conn = duckdb.connect(':memory:'); conn.execute('INSTALL sqlite; LOAD sqlite;'); print('DuckDB SQLite extension ready')"</automated>
  </verify>
  <done>DuckDB initialized and SQLite attachment capability verified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement "Shredding" Views</name>
  <files>src/services/analytical_service.py, tests/test_analytical.py</files>
  <action>
    - Create DuckDB views (or macros) to extract data from the `extractions` table's `extraction_json` column.
    - `view_summary`: Extracts `lender_name`, `total_amount`, and `effective_date` from the JSON triplets.
    - `view_transactions`: Uses DuckDB's JSON and array unnesting functions to create a flat table of individual transaction components and amounts.
    - Implement methods in `AnalyticalService` to fetch data from these views.
    - Create `tests/test_analytical.py` to verify the "shredding" logic with sample data.
  </action>
  <verify>
    <automated>pytest tests/test_analytical.py</automated>
  </verify>
  <done>Analytical views implemented and verified with tests.</done>
</task>

</tasks>

<verification>
Check if `AnalyticalService` returns flat dictionaries/lists for summary and transactions from a test database.
</verification>

<success_criteria>
- DuckDB attaches SQLite database successfully.
- `view_summary` returns correct flat fields for approved packages.
- `view_transactions` returns correctly unnested line items.
</success_criteria>

<output>
After completion, create .planning/phases/04-persistence/04-01-SUMMARY.md
</output>
