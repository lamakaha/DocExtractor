---
phase: 04-persistence
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [src/services/export_service.py, requirements.txt]
autonomous: true
requirements: [REQ-EXP-01, REQ-EXP-02]

must_haves:
  truths:
    - "System generates a valid multi-tab Excel file"
    - "Excel file contains Summary and Transactions tabs"
    - "Sheet names are sanitized according to Excel limits"
  artifacts:
    - path: "src/services/export_service.py"
      provides: "Excel generation logic using xlsxwriter"
  key_links:
    - from: "src/services/export_service.py"
      to: "xlsxwriter"
      via: "Excel file creation"
---

<objective>
Implement the Excel Export Service to generate professional multi-tab Excel reports from extracted data.
</objective>

<execution_context>
@C:/Users/sarka/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/services/analytical_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core Excel Export Logic</name>
  <files>requirements.txt, src/services/export_service.py</files>
  <action>
    - Add `xlsxwriter` to `requirements.txt`.
    - Implement `ExcelExporter` class in `src/services/export_service.py`.
    - Implement `generate_excel(packages_data: List[Dict]) -> BytesIO` method.
    - Implement sheet name sanitization (max 31 characters, remove `[]:*?/\\`).
  </action>
  <verify>
    <automated>python -c "import xlsxwriter; print('xlsxwriter ready')"</automated>
  </verify>
  <done>Core export logic and sanitization implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Multi-Tab Formatting and Itemization</name>
  <files>src/services/export_service.py, tests/test_export.py</files>
  <action>
    - Implement Tab 1 ("Summary"): Write flat fields (Package ID, Filename, Lender, Date, Total).
    - Implement Tab 2 ("Transactions"): Write unnested line items (Component, Amount).
    - Add formatting (bold headers, currency formats).
    - Create `tests/test_export.py` to generate a sample file and verify its structure.
  </action>
  <verify>
    <automated>pytest tests/test_export.py</automated>
  </verify>
  <done>Multi-tab formatting implemented and verified with tests.</done>
</task>

</tasks>

<verification>
Verify that generated Excel files can be opened and contain the expected tabs and data.
</verification>

<success_criteria>
- Multi-tab Excel file generated successfully.
- Summary tab contains correct aggregate data.
- Transactions tab contains all itemized line items.
- Sheet names are valid and sanitized.
</success_criteria>

<output>
After completion, create .planning/phases/04-persistence/04-02-SUMMARY.md
</output>
