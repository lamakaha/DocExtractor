---
phase: 01-ingestion
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [src/services/ingestor.py, scripts/verify_ingestion.py]
autonomous: true
requirements: [REQ-ING-01, REQ-NFR-01]

must_haves:
  truths:
    - "System limits recursion depth to prevent Zip Bomb attacks"
    - "System handles corrupted archives gracefully without crashing"
    - "End-to-end verification script confirms pipeline works for complex nested files"
  artifacts:
    - path: "scripts/verify_ingestion.py"
      provides: "CLI script to ingest a file and display extracted results"
  key_links:
    - from: "src/services/ingestor.py"
      to: "error logging"
      via: "logging module"
---

<objective>
Implement resilience measures such as recursion depth limits and error handling for corrupted files. Create an end-to-end verification script to demonstrate the full ingestion pipeline with sample files.

Purpose: Ensure the ingestion engine is robust and verifiable (REQ-NFR-01).
Output: Hardened ingestion service and verification CLI.
</objective>

<execution_context>
@C:/Users/sarka/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/sarka/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ingestion/01-02-SUMMARY.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Safety Limits and Corruption Handling</name>
  <files>src/services/ingestor.py</files>
  <action>
    Modify `RecursiveIngestor` to:
    - Add a `max_depth=5` limit (rejecting recursion deeper than this).
    - Add a `max_total_size` limit (e.g., 500MB) for extracted contents.
    - Wrap ZIP/EML extraction in try-except blocks.
    - On corruption: Log error, skip file, but continue with other files in the same archive.
    - Implement "Zip Slip" protection by normalizing file names (preventing directory traversal).
  </action>
  <verify>
    <automated>pytest tests/test_ingestor.py -k test_resilience</automated>
  </verify>
  <done>Resilience measures implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Create End-to-End Verification Script</name>
  <files>scripts/verify_ingestion.py</files>
  <action>
    Create a standalone script that:
    1. Takes a file path as an argument.
    2. Runs the ingestion pipeline (Package creation + recursive extraction).
    3. Queries the database and prints a tree-like summary of the extracted "Package".
    4. Shows counts of files, EML body text preview, and any errors encountered.
  </action>
  <verify>
    <automated>python scripts/verify_ingestion.py --help</automated>
  </verify>
  <done>Verification CLI script created.</done>
</task>

</tasks>

<verification>
Check that `max_depth` triggers correctly and `scripts/verify_ingestion.py` displays correct output for a sample nested file.
</verification>

<success_criteria>
1. ZIP bomb/Zip slip protections are active.
2. Max recursion depth is enforced.
3. Corrupted archives do not crash the pipeline.
4. CLI tool allows end-to-end verification of any archive file.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ingestion/01-03-SUMMARY.md`
</output>
