---
phase: 01-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [requirements.txt, src/models/schema.py, src/db/session.py]
autonomous: true
requirements: [REQ-ING-04]

must_haves:
  truths:
    - "Project scaffold exists with all necessary dependencies"
    - "SQLite database schema supports Packages and ExtractedFiles with Package ID grouping"
  artifacts:
    - path: "requirements.txt"
      provides: "Project dependencies (mail-parser, pydantic, sqlalchemy)"
    - path: "src/models/schema.py"
      provides: "SQLAlchemy models for Package and ExtractedFile"
    - path: "src/db/session.py"
      provides: "SQLite engine and session factory"
  key_links:
    - from: "src/models/schema.py"
      to: "SQLAlchemy"
      via: "Base and Column definitions"
---

<objective>
Set up the project foundation, including the directory structure, dependencies, and the SQLite data model for storing "Packages" and their extracted files.

Purpose: Provide the infrastructure for Phase 1 and ensure REQ-ING-04 is met.
Output: Initialized repository and database schema.
</objective>

<execution_context>
@C:/Users/sarka/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/sarka/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Project Scaffold and Dependencies</name>
  <files>requirements.txt, src/__init__.py, tests/__init__.py</files>
  <action>
    Create the project directory structure (src, tests, models, db, services). 
    Populate `requirements.txt` with:
    - `mail-parser` (for EML parsing)
    - `pydantic` (for data validation)
    - `sqlalchemy` (for SQLite ORM)
    - `pytest` (for testing)
    - `Pillow` (for PDF/Image handling in later phases)
    - `python-magic` (for MIME-type detection)
  </action>
  <verify>
    <automated>pip install -r requirements.txt</automated>
  </verify>
  <done>Dependencies installed, directory structure created.</done>
</task>

<task type="auto">
  <name>Task 2: Define Data Model and Database Session</name>
  <files>src/models/schema.py, src/db/session.py</files>
  <action>
    Implement SQLite schema using SQLAlchemy:
    - `Package`: id (UUID/String), original_filename, status (INGESTED, EXTRACTED, APPROVED), created_at.
    - `ExtractedFile`: id (Integer), package_id (FK), filename, original_path (nested path), content (Blob/Binary), extracted_text (Text - for EML bodies), mime_type, size.
    Set up the SQLite session factory in `src/db/session.py` with WAL mode enabled.
  </action>
  <verify>
    <automated>python -c "from src.models.schema import Package; from src.db.session import engine; Package.__table__.create(engine)"</automated>
  </verify>
  <done>Models defined and SQLite database initialized.</done>
</task>

</tasks>

<verification>
Check if `packages.db` exists and has the correct tables.
</verification>

<success_criteria>
1. All directories (src, tests, models, db) are created.
2. `requirements.txt` includes `mail-parser` and `sqlalchemy`.
3. Database schema supports `Package` and `ExtractedFile` with FK relationship.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ingestion/01-01-SUMMARY.md`
</output>
