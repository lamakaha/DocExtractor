---
phase: 05-ui-validation
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified: [tests/e2e/conftest.py, tests/e2e/test_reviewer.py]
autonomous: true
requirements: [REQ-UI-02]
must_haves:
  truths:
    - Reviewer test can navigate to the Reviewer view by clicking "Review" on a package.
    - Reviewer test interacts with the extraction form fields.
    - Reviewer test clicks "Approve" and verifies the status updates.
  artifacts:
    - path: tests/e2e/test_reviewer.py
      provides: E2E test cases for the Reviewer interface.
  key_links:
    - from: tests/e2e/test_reviewer.py
      to: src/ui/reviewer.py
      via: Playwright interactions
---

<objective>
Validate the Human-In-The-Loop Reviewer interface by simulating a user navigating from the dashboard, reviewing extracted data, modifying a field, and approving the package.

Purpose: Ensure the critical side-by-side review workflow is functional and free of frontend regressions.
Output: E2E test suite for the Reviewer view.
</objective>

<context>
@src/ui/reviewer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Reviewer Test Data</name>
  <files>tests/e2e/conftest.py</files>
  <action>
    Update `tests/e2e/conftest.py` to add a `seed_db_with_extraction` fixture (or update `seed_db`).
    Insert a Package in `EXTRACTED` status, along with a linked `Extraction` record containing a valid JSON `extraction_json` so the form has fields to render.
    Include a mock file record so the document viewer doesn't error out (an empty PDF or image bytes is fine).
  </action>
  <verify>
    <automated>pytest tests/e2e/test_dashboard.py --collect-only</automated>
  </verify>
  <done>Test data setup function includes extraction data.</done>
</task>

<task type="auto">
  <name>Task 2: Navigation and Form Interaction Test</name>
  <files>tests/e2e/test_reviewer.py</files>
  <action>
    Create `test_reviewer_approval_flow(page, seed_db_with_extraction)`.
    1. Navigate to Dashboard (`http://localhost:8502`), wait for ready.
    2. Click the "Review" button for the seeded package (`page.get_by_role("button", name="Review").first.click()`).
    3. Call `wait_for_app_ready(page)`.
    4. Assert the page header contains "Review:".
    5. Find one of the text inputs for an extracted field, fill it with a new value (`page.get_by_role("textbox").first.fill("New Value")`).
    6. Press Enter or click outside to trigger the Streamlit rerun, then call `wait_for_app_ready(page)`.
  </action>
  <verify>
    <automated>pytest tests/e2e/test_reviewer.py -v</automated>
  </verify>
  <done>Navigation and form interaction test passes without exceptions.</done>
</task>

<task type="auto">
  <name>Task 3: Approval Workflow Test</name>
  <files>tests/e2e/test_reviewer.py</files>
  <action>
    Extend the test from Task 2:
    1. Click the "Approve" button (`page.get_by_role("button", name="Approve").click()`).
    2. Call `wait_for_app_ready(page)`.
    3. The app should route back to the Dashboard. Verify that the package status in the UI is now `APPROVED`.
    4. Assert no `stException` occurred during the entire flow.
  </action>
  <verify>
    <automated>pytest tests/e2e/test_reviewer.py -v</automated>
  </verify>
  <done>Approval flow test passes and status change is reflected on the frontend.</done>
</task>

</tasks>

<verification>
Run `pytest tests/e2e/test_reviewer.py -v` to ensure the reviewer flow is completely verified.
</verification>

<success_criteria>
- Playwright navigates from Dashboard to Reviewer seamlessly.
- Text inputs can be modified without Streamlit hanging.
- Approval correctly updates the DB and returns the user to the Dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-validation/05-03-SUMMARY.md`
</output>
