---
phase: 05-ui-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [requirements.txt, tests/e2e/conftest.py, tests/e2e/test_smoke.py, tests/e2e/utils.py]
autonomous: true
requirements: [REQ-UI-01, REQ-UI-02]
must_haves:
  truths:
    - Playwright is installed and accessible via pytest.
    - Tests can start a dedicated Streamlit background process on port 8502.
    - E2E tests wait for Streamlit loading states before interacting.
  artifacts:
    - path: tests/e2e/conftest.py
      provides: Streamlit subprocess fixture and isolated DB setup.
    - path: tests/e2e/utils.py
      provides: Locator wait functions for Streamlit's stStatusWidget.
    - path: tests/e2e/test_smoke.py
      provides: Basic startup check ensuring no stException is present.
  key_links:
    - from: tests/e2e/conftest.py
      to: src/ui/app.py
      via: subprocess.Popen
---

<objective>
Establish the End-to-End testing infrastructure using pytest-playwright and ensure Streamlit can be launched and verified programmatically.

Purpose: To create a stable foundation for UI tests that doesn't conflict with development and handles Streamlit's dynamic reruns gracefully.
Output: Working test runner, utility functions, and a passing smoke test.
</objective>

<context>
@.planning/phases/05-ui-validation/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Dependencies</name>
  <files>requirements.txt</files>
  <action>
    Add `pytest-playwright` and `playwright` to `requirements.txt`.
    Run `pip install -r requirements.txt`.
    Run `playwright install chromium` (use `playwright install chromium --with-deps` if necessary).
  </action>
  <verify>
    <automated>pytest --version</automated>
  </verify>
  <done>Playwright dependencies are installed and available.</done>
</task>

<task type="auto">
  <name>Task 2: Create Test Infrastructure</name>
  <files>tests/e2e/conftest.py, tests/e2e/utils.py</files>
  <action>
    Create `tests/e2e/utils.py` with a helper function `wait_for_app_ready(page)` that waits for `[data-testid="stStatusWidget"]` to be hidden (Streamlit's loading indicator).
    
    Create `tests/e2e/conftest.py` with a session-scoped fixture `start_streamlit`.
    The fixture should:
    1. Set `DATABASE_URL=sqlite:///test_e2e.db` in the environment to isolate data.
    2. Set `OPENROUTER_API_KEY=dummy` in the environment so the Streamlit early-exit block is bypassed.
    3. Spawn Streamlit via `subprocess.Popen(["streamlit", "run", "src/ui/app.py", "--server.port=8502", "--server.headless=true"])`.
    4. Use `time.sleep(3)` to wait for startup, yield to tests, then terminate and wait for the process.
  </action>
  <verify>
    <automated>python -c "import tests.e2e.conftest"</automated>
  </verify>
  <done>Infrastructure files created with correct fixtures and utilities.</done>
</task>

<task type="auto">
  <name>Task 3: Create Smoke Test</name>
  <files>tests/e2e/test_smoke.py</files>
  <action>
    Create a simple test `test_no_streamlit_exceptions(page)` that:
    1. Navigates to `http://localhost:8502`.
    2. Calls `wait_for_app_ready(page)`.
    3. Asserts that `page.locator('[data-testid="stException"]').is_visible()` is False (or uses `expect(locator).not_to_be_attached()`).
  </action>
  <verify>
    <automated>pytest tests/e2e/test_smoke.py -v</automated>
  </verify>
  <done>Smoke test passes and Streamlit server is properly spawned and terminated.</done>
</task>

</tasks>

<verification>
Run the smoke test to confirm the Streamlit fixture works and Chromium can connect.
</verification>

<success_criteria>
- `pytest-playwright` is installed.
- `pytest tests/e2e/test_smoke.py` passes.
- Streamlit does not hang during test teardown.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-validation/05-01-SUMMARY.md`
</output>
