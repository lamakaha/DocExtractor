---
phase: 05-ui-validation
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [tests/e2e/test_dashboard.py, tests/e2e/conftest.py]
autonomous: true
requirements: [REQ-UI-01]
must_haves:
  truths:
    - Tests can seed the isolated E2E database with test packages.
    - Dashboard E2E test verifies packages render in the UI table.
    - Export flow E2E test successfully triggers export download.
  artifacts:
    - path: tests/e2e/test_dashboard.py
      provides: E2E test cases for Dashboard features.
  key_links:
    - from: tests/e2e/test_dashboard.py
      to: src/ui/dashboard.py
      via: Playwright interactions
---

<objective>
Validate the Dashboard's core functionalities: displaying packages and triggering the Excel Export flow without encountering frontend errors.

Purpose: Ensure the main entry point of the app functions correctly under automated interaction.
Output: E2E test suite for the Dashboard.
</objective>

<context>
@src/ui/dashboard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DB Seeding Fixture</name>
  <files>tests/e2e/conftest.py</files>
  <action>
    Update `tests/e2e/conftest.py` to add a function-scoped fixture `seed_db` that initializes `sqlite:///test_e2e.db` and inserts a few dummy `Package` records (e.g., one INGESTED, one APPROVED) directly via SQLAlchemy so the UI has data to display.
    Ensure to clean up or reset the DB before each test run.
  </action>
  <verify>
    <automated>pytest tests/e2e/test_smoke.py --collect-only</automated>
  </verify>
  <done>DB seeding fixture is available for tests.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard UI Test</name>
  <files>tests/e2e/test_dashboard.py</files>
  <action>
    Create `test_dashboard_renders_packages(page, seed_db)`.
    Navigate to `http://localhost:8502`, call `wait_for_app_ready(page)`.
    Assert that the dummy package statuses (e.g., `APPROVED`) appear on the page (e.g., `expect(page.get_by_text("APPROVED")).to_be_visible()`).
    Assert no `stException` is present on the page.
  </action>
  <verify>
    <automated>pytest tests/e2e/test_dashboard.py::test_dashboard_renders_packages -v</automated>
  </verify>
  <done>Test passes when dashboard renders DB contents correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Export Flow Test</name>
  <files>tests/e2e/test_dashboard.py</files>
  <action>
    Create `test_dashboard_export_flow(page, seed_db)`.
    Navigate to `http://localhost:8502`, call `wait_for_app_ready(page)`.
    Click the "Prepare Bulk Export" button (`page.get_by_role("button", name="Prepare Bulk Export").click()`).
    Call `wait_for_app_ready(page)`.
    Verify the "⬇️ Download Approved Excel" button appears (`expect(page.get_by_role("button", name="⬇️ Download Approved Excel")).to_be_visible()`).
  </action>
  <verify>
    <automated>pytest tests/e2e/test_dashboard.py::test_dashboard_export_flow -v</automated>
  </verify>
  <done>Export flow test passes without exceptions.</done>
</task>

</tasks>

<verification>
Run `pytest tests/e2e/test_dashboard.py -v` to ensure all dashboard tests pass.
</verification>

<success_criteria>
- DB seeding correctly populates the E2E database.
- Dashboard test finds the seeded packages.
- Export test successfully interacts with the export buttons.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-validation/05-02-SUMMARY.md`
</output>
