---
phase: 03-reviewer
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [src/models/schema.py, src/ui/reviewer.py, src/ui/db_utils.py]
autonomous: false
requirements: [REQ-UI-03, REQ-UI-04, REQ-UI-05]

must_haves:
  truths:
    - "User can see a dynamic form populated with extracted triplets."
    - "Fields are color-coded based on confidence scores (Red < 50%, Yellow < 80%, Green >= 80%)."
    - "Selecting a field highlights its corresponding bounding box on the document image."
    - "User can edit field values and save corrections."
    - "Approving a package updates its status to 'APPROVED' and marks extractions as reviewed."
  artifacts:
    - path: "src/ui/reviewer.py"
      provides: "Dynamic form, visual grounding sync, and approval logic"
    - path: "src/models/schema.py"
      provides: "Updated Extractions model with is_reviewed column"
  key_links:
    - from: "src/ui/reviewer.py"
      to: "src/ui/db_utils.py"
      via: "update_extraction and approve_package calls"
    - from: "src/ui/reviewer.py"
      to: "st_canvas"
      via: "drawing active_bbox on focus"
---

<objective>
Implement the dynamic review form, visual grounding (BBox synchronization), and the final approval workflow.
This completes the HITL (Human-In-The-Loop) interface, enabling users to verify and correct AI-extracted data.
</objective>

<execution_context>
@C:/Users/sarka/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-reviewer/03-02-SUMMARY.md
@src/models/schema.py
@src/models/triplets.py
@src/ui/reviewer.py
@src/services/coordinate_scaler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema Update & DB Utils Extension</name>
  <files>src/models/schema.py, src/ui/db_utils.py</files>
  <action>
    - Add `is_reviewed = Column(Boolean, default=False)` to the `Extractions` table in `src/models/schema.py`.
    - Update `src/ui/db_utils.py` to include:
      - `update_extraction(extraction_id, updated_json, is_reviewed=True)`: Updates the JSON and marks as reviewed.
      - `update_package_status(package_id, status)`: Updates the Package status (e.g., to 'APPROVED').
    - Create a small migration script (or run via Task 1 verify) to ensure the column exists in the local `packages.db`.
  </action>
  <verify>
    <automated>python -c "from src.models.schema import Extractions; print('is_reviewed' in Extractions.__table__.columns)"</automated>
  </verify>
  <done>Database schema updated and helper functions implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Dynamic Form & Color Coding</name>
  <files>src/ui/reviewer.py</files>
  <action>
    - In `src/ui/reviewer.py`, implement dynamic form generation within the right column of `show_reviewer`.
    - Parse `Extractions.extraction_json` into a dictionary of `Triplet` objects.
    - For each field:
      - Determine color based on confidence: Green (>= 0.8), Yellow (0.5-0.79), Red (< 0.5).
      - Render a colored indicator (e.g., using `st.markdown` with a colored dot or a colored container).
      - Render `st.text_input` for the field value.
      - Track the focused field using `st.session_state.active_field`. Since Streamlit doesn't have native "onFocus", use a "Locate" button next to each field or set `active_field` if the input changes.
    - Implement a mechanism to update `st.session_state.active_bbox` when a field is focused/selected.
  </action>
  <verify>
    <automated>python -c "from src.ui.reviewer import show_reviewer; print('Form logic implemented')"</automated>
  </verify>
  <done>Reviewer form is dynamic, color-coded, and tracks active field for grounding.</done>
</task>

<task type="auto">
  <name>Task 3: Visual Grounding & Approval Workflow</name>
  <files>src/ui/reviewer.py</files>
  <action>
    - Update the `st_canvas` rendering in `src/ui/reviewer.py` to draw a high-contrast rectangle (BBox) if `st.session_state.active_bbox` is present.
    - Use `coordinate_scaler.pixel_to_canvas` (from Plan 02) to scale the triplet's 0-1000 coordinates to the canvas display size.
    - Add an "Approve" button at the bottom of the form.
    - On click:
      - Collect all values from the form inputs.
      - Re-construct the `extraction_json` with updated values.
      - Call `update_extraction` and `update_package_status(package_id, 'APPROVED')`.
      - Set `st.session_state.current_view = 'Dashboard'` to redirect.
      - Show a success toast or notification.
  </action>
  <verify>
    <automated>python -c "from src.ui.reviewer import show_reviewer; print('Approval logic implemented')"</automated>
  </verify>
  <done>Visual grounding works and the approval workflow correctly persists changes and updates status.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete HITL Reviewer Interface.</what-built>
  <how-to-verify>
    1. Run: streamlit run src/ui/app.py
    2. Select an EXTRACTED package.
    3. Verify fields are color-coded (Red/Yellow/Green).
    4. Click "Locate" (or use the focus mechanism) on a field and ensure a BBox appears on the document image at the correct location.
    5. Edit a field value.
    6. Click "Approve".
    7. Verify redirection to Dashboard and that the package status is now "APPROVED".
    8. (Optional) Check the database to ensure `extraction_json` contains the edited value and `is_reviewed` is True.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Verify end-to-end review flow from Dashboard -> Reviewer -> Edit -> Approve -> Dashboard.
</verification>

<success_criteria>
- Dynamic form matches extracted document fields.
- Confidence scores are visually indicated via color coding.
- BBox highlights the correct area of the document on field focus.
- Manual corrections are saved to the database.
- Package status updates to APPROVED after review.
</success_criteria>

<output>
After completion, create .planning/phases/03-reviewer/03-03-SUMMARY.md
</output>
