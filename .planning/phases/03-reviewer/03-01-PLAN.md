---
phase: 03-reviewer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [requirements.txt, src/ui/app.py, src/ui/dashboard.py, src/ui/db_utils.py]
autonomous: false
requirements: [REQ-UI-01]

must_haves:
  truths:
    - "User can view a dashboard table showing all packages in the database."
    - "User can filter packages by status (INGESTED, EXTRACTED, APPROVED)."
    - "User can select a package to begin the review process."
  artifacts:
    - path: "src/ui/app.py"
      provides: "Main Streamlit entry point and layout"
    - path: "src/ui/dashboard.py"
      provides: "Dashboard view with package table"
    - path: "src/ui/db_utils.py"
      provides: "Database access functions for Streamlit"
  key_links:
    - from: "src/ui/app.py"
      to: "src/ui/dashboard.py"
      via: "import and call"
    - from: "src/ui/dashboard.py"
      to: "src/ui/db_utils.py"
      via: "function calls for data fetching"
---

<objective>
Initialize the Streamlit app structure and implement the Package Dashboard.
This establishes the foundation for the HITL UI, allowing users to browse and select packages based on their processing status.
</objective>

<execution_context>
@C:/Users/sarka/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/models/schema.py
@src/db/session.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Setup & UI Entry Point</name>
  <files>requirements.txt, src/ui/app.py, src/ui/__init__.py</files>
  <action>
    - Add 'streamlit' and 'streamlit-drawable-canvas' to requirements.txt.
    - Create src/ui/app.py as the main Streamlit entry point.
    - Implement a basic sidebar for navigation (Dashboard, Reviewer) and a placeholder for the main view.
    - Use st.set_page_config for a professional title and wide layout.
    - Initialize st.session_state for 'current_package_id' and 'current_view'.
  </action>
  <verify>
    <automated>python -m streamlit run src/ui/app.py --server.headless=true --server.runOnSave=false & sleep 5; curl http://localhost:8501 | grep -i "Streamlit"</automated>
  </verify>
  <done>Streamlit app starts and renders basic navigation.</done>
</task>

<task type="auto">
  <name>Task 2: DB Access & Dashboard Implementation</name>
  <files>src/ui/db_utils.py, src/ui/dashboard.py, tests/test_ui_queries.py</files>
  <action>
    - Implement src/ui/db_utils.py to provide helper functions using sqlalchemy for:
      - get_all_packages() -> List[Package]
      - get_package_by_id(id) -> Package
      - get_extractions_for_package(id) -> List[Extractions]
    - Implement src/ui/dashboard.py to render a table of packages.
    - Include columns: ID, Filename, Status, Created At.
    - Add status filtering in the UI (multiselect or radio).
    - Add a "Review" button in each row (using st.button or st.dataframe with selection) to set current_package_id and change view to 'Reviewer'.
    - Create tests/test_ui_queries.py to verify DB access functions.
  </action>
  <verify>
    <automated>pytest tests/test_ui_queries.py</automated>
  </verify>
  <done>Dashboard displays packages from the database and allows selection.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Streamlit Dashboard with package table.</what-built>
  <how-to-verify>
    1. Run: streamlit run src/ui/app.py
    2. Open the URL in a browser.
    3. Ensure a list of packages is visible (or an empty state message).
    4. Test filtering by status.
    5. Click "Review" on a package (if any exist) and verify the view changes (even if Reviewer is currently a placeholder).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Check dashboard renders and filters correctly against packages.db.
</verification>

<success_criteria>
- Dashboard shows a table of packages with ID, Status, and Filename.
- Filtering by INGESTED, EXTRACTED, APPROVED works.
- Selecting a package updates session state and transitions view.
</success_criteria>

<output>
After completion, create .planning/phases/03-reviewer/03-01-SUMMARY.md
</output>
