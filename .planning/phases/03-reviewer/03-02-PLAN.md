---
phase: 03-reviewer
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [src/ui/reviewer.py, src/services/coordinate_scaler.py]
autonomous: false
requirements: [REQ-UI-02, REQ-UI-03, REQ-UI-05]

must_haves:
  truths:
    - "User can see a side-by-side view with document image on left and form on right."
    - "Document image is rendered correctly in a canvas component."
    - "Coordinates stored in database are correctly scaled to the canvas display size."
  artifacts:
    - path: "src/ui/reviewer.py"
      provides: "Reviewer interface with side-by-side layout"
    - path: "src/services/coordinate_scaler.py"
      provides: "Pixel-to-Canvas scaling logic"
  key_links:
    - from: "src/ui/reviewer.py"
      to: "streamlit-drawable-canvas"
      via: "st_canvas component"
    - from: "src/ui/reviewer.py"
      to: "src/services/coordinate_scaler.py"
      via: "scaling calls"
---

<objective>
Implement the core Reviewer interface with visual document rendering and coordinate scaling.
This allows the user to see the original document and prepares for visual grounding of extraction fields.
</objective>

<execution_context>
@C:/Users/sarka/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-reviewer/03-01-SUMMARY.md
@src/models/schema.py
@src/services/coordinate_scaler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Side-by-Side Reviewer Layout</name>
  <files>src/ui/reviewer.py, tests/test_reviewer_logic.py</files>
  <action>
    - Create src/ui/reviewer.py with a show_reviewer(package_id) function.
    - Implement side-by-side layout using st.columns([2, 1]) for Image (left) vs Form (right).
    - Add page navigation (Prev/Next) if multiple extractions/pages exist for the package.
    - Fetch the corresponding ExtractedFile (image) for the current extraction record.
    - Create tests/test_reviewer_logic.py to verify data fetching and layout state logic.
  </action>
  <verify>
    <automated>pytest tests/test_reviewer_logic.py</automated>
  </verify>
  <done>Reviewer layout is structured with space for image and form.</done>
</task>

<task type="auto">
  <name>Task 2: Canvas Rendering & Canvas Scaling</name>
  <files>src/ui/reviewer.py, src/services/coordinate_scaler.py, tests/test_scaler.py</files>
  <action>
    - Update src/services/coordinate_scaler.py with a new static method:
      - pixel_to_canvas(bbox_pixels, img_width, img_height, canvas_width) -> List[int] (scales pixels to canvas display size).
    - Update tests/test_scaler.py to include unit tests for the new pixel_to_canvas method.
    - In src/ui/reviewer.py, implement st_canvas to display the document image as background.
    - Calculate canvas_width dynamically based on Streamlit column width (or a fixed reasonable default like 700).
    - Set drawing_mode="transform" and display_toolbar=False for the initial view.
  </action>
  <verify>
    <automated>pytest tests/test_scaler.py</automated>
  </verify>
  <done>Document image renders on the canvas with correct aspect ratio and display scaling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Document Image Canvas rendering.</what-built>
  <how-to-verify>
    1. From the Dashboard, select a package with "EXTRACTED" status.
    2. Verify the document image appears on the left side.
    3. Ensure the image is not distorted and fits within the column.
    4. Check if page navigation (if applicable) updates the image.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Check image rendering and scaling accuracy.
</verification>

<success_criteria>
- Reviewer shows the original document image using st_canvas.
- Image fits the layout column correctly.
- Coordinate scaler has pixel_to_canvas method and unit tests pass.
</success_criteria>

<output>
After completion, create .planning/phases/03-reviewer/03-02-SUMMARY.md
</output>
